{
  "name": "functional-emotions-eval",
  "version": "0.2.0",
  "description": "Local-only data-collection-and-eval pipeline for functional-emotions",
  "type": "module",
  "private": true,
  "scripts": {
    "ingest": "bun run ingest.ts",
    "report": "bun run report.ts",
    "label": "bun run label.ts",
    "restore-labels": "bun run restore-labels.ts",
    "score:reward-hack": "bun run scorers/reward_hack_v1.ts",
    "score:premature-confidence": "bun run scorers/premature_confidence_v1.ts"
  },
  "dependencies": {
    "@duckdb/node-api": "1.5.2-r.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
